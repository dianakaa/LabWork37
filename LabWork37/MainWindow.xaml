<Window x:Class="LabWork37.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LabWork37"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchTextBox" TextChanged="SearchTextBox_TextChanged"/>
        <DataGrid Grid.Row="1" x:Name="FilesDataGrid" AutoGenerateColumns="True"/>
        <TextBlock Grid.Row="2" x:Name="RecordCountTextBlock"/>
        <Button x:Name="ResetFilterButton" Grid.Row="4" Content="Сбросить фильтр" Click="ResetFilterButton_Click"/>
    </Grid>
</Window>
